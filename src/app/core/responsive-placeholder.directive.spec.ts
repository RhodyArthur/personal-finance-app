import { ResponsivePlaceholderDirective } from './responsive-placeholder.directive';

describe('ResponsivePlaceholderDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsivePlaceholderDirective();
    expect(directive).toBeTruthy();
  });
});
